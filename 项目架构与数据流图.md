# LinkedIn 爬虫项目架构与数据流图

## 1) 模块依赖图

```mermaid
flowchart TD
    A[main.py 入口] --> B[src/cli.py 交互层]
    B --> C[src/main_controller.py 控制器]

    C --> U1[src/utils/config_loader.py]
    C --> U2[src/utils/logger.py]
    C --> U3[src/utils/helpers.py]
    C --> U4[src/utils/llm_client.py]

    C --> S1[src/scraper/browser_manager.py]
    C --> S2[src/scraper/linkedin_scraper.py]
    S2 --> S3[src/scraper/linkedin_search_manager.py]
    S3 --> S4[src/scraper/extractors/cdp_dom_extractor.py]
    S3 --> S5[src/scraper/llm_refiner.py]

    C --> F1[src/filter/contact_filter.py]
    F1 --> F2[src/filter/embedding_filter.py]

    C --> D1[src/data_manager/database_manager.py]
    C --> D2[src/data_manager/contact_manager.py]

    C --> M1[src/marketing/automation.py]
    M1 --> M2[src/marketing/auto_connect.py]
    M1 --> D1
```

## 2) 核心时序图（init/start/scrape/filter/connect）

```mermaid
sequenceDiagram
    participant User as 用户
    participant CLI as CLI
    participant MC as MainController
    participant BM as BrowserManager
    participant SM as SearchManager
    participant EX as CDPExtractor
    participant DB as ContactManager/Database
    participant CF as ContactFilter
    participant EF as EmbeddingFilter
    participant LLM as 智谱GLM/Embedding

    User->>CLI: init
    CLI->>MC: initialize_system()
    MC->>DB: 初始化数据库/目录
    MC-->>CLI: 初始化完成

    User->>CLI: start
    CLI->>MC: start_browser_session()
    MC->>BM: setup_driver() + navigate_to_linkedin()
    BM-->>MC: 浏览器会话就绪

    User->>CLI: scrape --keywords --pages
    CLI->>MC: scrape_keywords()
    MC->>SM: search_people(keyword, max_pages)
    loop 每页
        SM->>SM: 页面健康检查/重试
        SM->>EX: execute_script 提取联系人
    end
    SM-->>MC: 联系人列表
    MC->>DB: add_contacts() 入库
    MC-->>CLI: 爬取统计

    User->>CLI: filter
    CLI->>CF: filter_contacts(contacts, requirements)
    CF->>EF: enhance_requirement()
    EF->>LLM: 需求增强（扩展关键词库）
    CF->>EF: compute_requirement_embedding()
    EF->>LLM: 计算需求向量
    CF->>EF: compute_contact_embeddings()
    EF->>LLM: 批量计算联系人向量（分批处理）
    EF-->>CF: filtered_contacts + stats
    CF-->>CLI: 筛选结果

    User->>CLI: export
    CLI->>DB: 导出联系人数据
```

## 3) 数据流图（raw → DB → filter → processed）

```mermaid
flowchart LR
    A[LinkedIn 页面DOM] --> B[data/raw/_sessions/search_results_*]
    B --> C[contacts 表]
    C --> D[Embedding Filter 评分]
    D --> E[data/filter_results/run_*/filter_result.json]
    E --> F[export json/csv]
```

## 4) Embedding 筛选引擎架构

```mermaid
flowchart TD
    A[用户需求描述] --> B[需求增强 LLM]
    B --> C[扩展关键词库]
    C --> D[需求向量计算]

    E[联系人数据] --> F[分批处理 batch_size=50]
    F --> G[联系人向量计算]

    D --> H[余弦相似度计算]
    G --> H

    H --> I[job_score = 规则0.4 + 向量0.6]
    H --> J[company_score = 规则0.5 + 向量0.5]

    I --> K[综合评分]
    J --> K
    C --> L[规则匹配]
    L --> K

    K --> M[匹配阈值过滤 min_score=0.3]
```

## 5) 批量处理关键配置

- `embedding_model.batch_size`: 50（智谱API限制单次≤64条）
- `embedding_model.timeout`: 60秒
- `embedding_model.dimensions`: 512（向量维度）
- `llm_enhancement.enabled`: true（需求增强开关）
- `llm_enhancement.timeout`: 60秒

## 6) 评分权重配置

| 分数项 | 权重 | 说明 |
|--------|------|------|
| embedding_similarity | 0.3 | 向量相似度 |
| company_match | 0.4 | 公司匹配（规则+向量） |
| job_keywords | 0.3 | 职位匹配（规则+向量） |

最终评分公式：
```
job_score = job_rule * 0.4 + similarity * 0.6
company_score = company_rule * 0.5 + similarity * 0.5
match_score = similarity * 0.3 + company_score * 0.4 + job_score * 0.3
```